# tron-grid-dots — if boot rotation did not stick, apply it before tuigreet.
# install.sh copies this to /etc/systemd/system/greetd.service.d/90-tron-rotate.conf
[Service]
ExecStartPre=-/usr/local/bin/tron-orient --fbcon
